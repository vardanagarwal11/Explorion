# Required by manim-voiceover (pkg_resources) - MUST BE FIRST
setuptools>=65.0.0
# Bundled ffmpeg binary for Windows (no system ffmpeg needed)
imageio-ffmpeg>=0.4.9

# API framework
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6

# Core validation
pydantic>=2.0.0
python-dotenv>=0.21.1

# Async / transport
httpx>=0.25.0
asyncio-throttle>=1.0.0

# Database / jobs
sqlalchemy>=2.0.0
aiosqlite>=0.19.0
asyncpg>=0.29.0
psycopg2-binary>=2.9.0
greenlet>=3.0.0

# Ingestion
arxiv>=2.0.0
pymupdf4llm>=0.0.5
pymupdf>=1.23.0
beautifulsoup4>=4.12.0
lxml>=5.0.0
pypdf>=4.0.0

# Rendering / visualization
manim>=0.18.0
manim-voiceover[gtts]>=0.3.0
modal>=0.60.0

# Cloud storage (S3-compatible — Cloudflare R2)
boto3>=1.34.0

# LLM providers (all free-tier APIs)
openai>=1.12.0          # Used for NIM + Groq (both OpenAI-compatible)
groq>=1.0.0             # Optional: Groq-native client

# PDF upload support
groq>=1.0.0

# Audio post-processing
pydub>=0.25.0
tenacity>=8.0.0

# Overlap detection
opencv-python>=4.8.0

# Tests
pytest>=7.0.0
pytest-asyncio>=0.21.0

# Solana Support
solana>=0.30.2
solders>=0.18.0
